import { expect, test } from 'vitest'

// Smoke test to confirm the Vitest harness is wired up.
// Replace with real unit/component tests as features land.
test('vitest harness works', () => {
  expect(1 + 1).toBe(2)
})
